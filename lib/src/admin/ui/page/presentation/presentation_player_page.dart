import 'package:flutter/material.dart';

class PresentationPlayerPage extends StatelessWidget {
  const PresentationPlayerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Atletas')));
  }
}
