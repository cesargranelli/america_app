import 'package:flutter/material.dart';

class PresentationConferencePage extends StatelessWidget {
  const PresentationConferencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Conferências')));
  }
}
