import 'package:flutter/material.dart';

class PresentationDivisionalPage extends StatelessWidget {
  const PresentationDivisionalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Divisões')));
  }
}
