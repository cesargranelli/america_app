import 'package:flutter/material.dart';

class PresentationTeamPage extends StatelessWidget {
  const PresentationTeamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Agremiações')));
  }
}
